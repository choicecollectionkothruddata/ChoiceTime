/** Single source for amounts used in Razorpay (paise). */
export const COD_ADVANCE_PAISE = 100; // ₹1 — must match policy / frontend
/** Deducted from full online refund when order was already shipped (or delivered). */
export const ONLINE_CANCEL_SHIPPED_DEDUCTION_PAISE = 20000; // ₹200
